<view class='expander {{expanded ? "expanded" : ""}}'>
    <view class='header' bindtap='toggle'>
        <image class='icon' src='{{icon}}'></image>
        {{header}}
    </view>
    <view class='content'>
        <slot></slot>
    </view>
</view>